name: "Build Website"
on:
  pull_request:
  push:
jobs:
  build-website:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2.4.0
    - uses: cachix/install-nix-action@v15
    - run: nix flake check
    - run: nix run .#hugo
